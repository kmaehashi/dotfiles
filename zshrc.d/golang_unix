# GOROOT
[ -z "${GOROOT}" -a -d ~/local/go ] && export GOROOT=~/local/go
[ ! -z "${GOROOT}" ] && export PATH="${GOROOT}/bin:${PATH}"

# GOPATH
[ -z "${GOPATH}" -a -d ~/.go ] && export GOPATH=~/go
[ ! -z "${GOPATH}" ] && export PATH="${GOPATH}/bin:${PATH}"
